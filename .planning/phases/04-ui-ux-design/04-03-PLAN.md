---
phase: 04-ui-ux-design
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - docs/WIREFRAMES.md
autonomous: true
requirements: [REQ-035]
must_haves:
  truths:
    - "An engineer can find a lo-fi wireframe for every distinct screen AND state (22+ wireframes) with enough layout detail to implement without design guesses"
    - "The 4-tab bottom bar (REQ-035) appears in every main-app wireframe with active/inactive states shown"
    - "The Settings (Admin) wireframe shows the 'Delete my data' row clearly and reachable within two taps (REQ-014)"
    - "The onboarding consent wireframe shows the exact REQ-009 self-attestation checkbox copy"
  artifacts:
    - path: "docs/WIREFRAMES.md"
      provides: "ASCII lo-fi wireframes for all screens/states"
      contains: "Group F"
      min_lines: 300
  key_links:
    - from: "docs/WIREFRAMES.md"
      to: ".planning/phases/04-ui-ux-design/04-UI-SPEC.md"
      via: "Ref: lines cross-referencing component specs"
      pattern: "Ref: UI-SPEC"
---

<objective>
Produce `docs/WIREFRAMES.md` — ASCII lo-fi wireframes for all 24 distinct screen/state variants, each annotated with typography, color, elevation, and a11y callouts plus a `Ref:` cross-reference back to the relevant 04-UI-SPEC.md component sections. Phase 5 engineers implement each screen directly from its wireframe + the referenced component spec.

Purpose: ROADMAP Phase 4 Success Criterion 2 requires a wireframe for every distinct screen AND state. The most common failure is undercounting states (Pitfall 2) — this plan uses the 24-item Screen Inventory as an explicit checklist split across two tasks.
Output: `docs/WIREFRAMES.md` with six groups (A–F) covering 24 wireframes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux-design/04-UI-SPEC.md
@.planning/phases/04-ui-ux-design/04-PATTERNS.md
@docs/DESIGN-TOKENS.md
@docs/03-system-architecture.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wireframe Groups A–C (Auth+Org, Onboarding, Home) — wireframes 1–14</name>
  <files>docs/WIREFRAMES.md</files>
  <read_first>
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Screen Inventory lines 501–597; §Component Inventory lines 301–497 for Log Button, Status Chips, Toast, Child Switcher; §Copywriting Contract lines 171–245 for CTA labels, consent copy, empty-state copy, error copy)
    - .planning/phases/04-ui-ux-design/04-PATTERNS.md (WIREFRAMES.md per-wireframe structure lines 153–214; ASCII box convention lines 246–251; annotation `·` bullet style lines 202–213; Ref-line format lines 254–259)
    - .planning/phases/04-ui-ux-design/04-RESEARCH.md (Screen Inventory full list lines 295–333; Pattern 3 ASCII example lines 249–293)
    - docs/DESIGN-TOKENS.md (token names to cite in annotations — color.primary, Title 20sp, radius.pill, etc.)
    - .planning/REQUIREMENTS.md (REQ-009 consent checkbox exact copy; REQ-036 wizard steps; REQ-035 tab bar)
  </read_first>
  <action>
    Create docs/WIREFRAMES.md. Open with `# Wireframes — OneStepTwo` H1, then a `>` blockquote citing 04-UI-SPEC.md §Screen Inventory + §Component Inventory, `> Last updated: 2026-06-29`, and a note that this file contains 24 distinct screen/state wireframes. No YAML front matter.

    Produce the first three groups using `## Group {X} — {name}` headings and `### {Screen} — {State}` per wireframe. Each wireframe is a fenced plain ` ``` ` ASCII box (40–42 char width, `┌─┐ │ └─┘ ├─┤` borders) followed by `·`-prefixed annotation bullets (typography level, color token, elevation, a11y contentDescription/role/min-tap-target) and a closing `Ref: UI-SPEC §… (component N)` line.

    `## Group A — Auth + Org Screens` (6 wireframes): 1) Sign In — default; 2) Sign In — error / OTP step (show error copy "Incorrect email or password. Try again." and OTP entry); 3) Sign Up — default + error; 4) Org Picker — list state; 5) Org Picker — loading / error states; 6) Invite Caregiver — default + success/error (CTA "Send invite").

    `## Group B — Onboarding Wizard` (4 wireframes, tab bar hidden throughout, step dots 1–5 at top): 7) Step 2 — Family name input (CTA "Continue setup"); 8) Step 3 — Child nickname + birth month/year selectors (CTA "Continue setup"); 9) Step 4 — Consent screen showing plain-language data explanation in Body 16sp and the self-attestation checkbox with the EXACT REQ-009 copy "I confirm I am the parent or legal guardian of this child and am 18 years of age or older" and CTA "I agree — continue" disabled until checked; 10) Step 5 — Success/completion (child name shown, CTA "Start tracking").

    `## Group C — Home Tab + Overlays` (4 wireframes): 11) Home — single child, no chips (child name header with NO chevron, log button as primary focal point, tab bar with Home active); 12) Home — multi-child (chevron "›" visible, both status chips `[N need details]` + `[N syncing…]` active); 13) Toast — body "Logged. Add a type?" with 6 event-type quick-pick chips (Pee, Poo, Both, Accident (pee), Accident (poo), Tried) above tab bar; 14) Home — empty state (new caregiver, no chips, log button still present).

    Every Group C wireframe must show the 4-tab bottom bar (Home | History | Progress | Settings) with the active tab marked and an annotation noting active = color.primary, inactive = color.on-surface 60% (REQ-035). Use `##`/`###` only — no `####`.
  </action>
  <verify>
    <automated>test -f docs/WIREFRAMES.md && [ "$(grep -c '^### ' docs/WIREFRAMES.md)" -ge 14 ] && grep -q 'parent or legal guardian' docs/WIREFRAMES.md && grep -q 'Logged. Add a type?' docs/WIREFRAMES.md && [ "$(grep -c 'Ref: UI-SPEC' docs/WIREFRAMES.md)" -ge 14 ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### ' docs/WIREFRAMES.md` returns ≥ 14 after this task (Groups A–C complete)
    - File contains the exact string `I confirm I am the parent or legal guardian of this child and am 18 years of age or older` (REQ-009)
    - File contains `Logged. Add a type?` and all six chip labels: `Pee`, `Poo`, `Both`, `Accident (pee)`, `Accident (poo)`, `Tried`
    - File contains the CTA labels `Continue setup`, `I agree — continue`, `Start tracking`, `Send invite`
    - `grep -c 'Ref: UI-SPEC' docs/WIREFRAMES.md` returns ≥ 14 (one cross-ref per wireframe)
    - At least one Home wireframe shows all four tab labels `Home`, `History`, `Progress`, `Settings`
    - `grep -c '^#### ' docs/WIREFRAMES.md` returns 0
  </acceptance_criteria>
  <done>Groups A–C (14 wireframes) exist in docs/WIREFRAMES.md, each with ASCII layout, annotations, and a UI-SPEC Ref line; consent copy and toast chips are verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Wireframe Groups D–F (History, Progress+Settings, Sheets+States) — wireframes 15–24</name>
  <files>docs/WIREFRAMES.md</files>
  <read_first>
    - docs/WIREFRAMES.md (file from Task 1 — append, preserve existing groups)
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§History Tab lines 550–568; §Progress Tab lines 570–582; §Settings Tab Admin lines 583–592; §Settings Tab Caregiver lines 594–596; §Component Inventory Heatmap Cell / Milestone Badge / Event Detail Sheet / Child Switcher lines 370–470; §Destructive Action Confirmations lines 233–242)
    - .planning/phases/04-ui-ux-design/04-PATTERNS.md (per-wireframe structure + Ref-line format)
    - .planning/phases/04-ui-ux-design/04-RESEARCH.md (Progress Tab ASCII example lines 492–543)
    - docs/DESIGN-TOKENS.md (heatmap intensity tokens, color.error for Delete my data)
    - .planning/REQUIREMENTS.md (REQ-014 delete flow reachability; REQ-033 heatmap; REQ-034 progress; REQ-031 child switcher)
  </read_first>
  <action>
    Append the final three groups to docs/WIREFRAMES.md, same wireframe structure (ASCII box + `·` annotations + `Ref:` line) as Task 1.

    `## Group D — History Tab` (3 wireframes): 15) History — heatmap with data (rolling GitHub-style grid, purple intensity tints empty/low/medium/high, week labels, month labels, legend "less → swatches → more"); 16) History — empty state ("No events yet" / "Log your first potty trip to see it here."); 17) History Day-Detail — chronological compact event-card list (timestamp + type icon + note, no caregiver name), tab bar HIDDEN, push navigation with platform back gesture, date header Headline 24sp.

    `## Group E — Progress + Settings` (4 wireframes): 18) Progress Tab — with data (active child name, current streak Display 28sp color.primary + "day streak", "Best: N days", This week / All time stats Title 20sp, 2×2 milestone badge grid with at least one UNLOCKED and one locked badge, badges 64×64 circles); 19) Progress Tab — empty state ("[Child name] is just getting started" / "Keep logging to see streaks and milestones."); 20) Settings Tab — Admin (4 sections: Family with members list + "Invite caregiver" row + remove-caregiver destructive action, Children with "Add child" + edit/remove, Notifications toggle per child, Account with Account info + Sign out + a clearly-labelled "Delete my data" row in color.error reachable within two taps — REQ-014); 21) Settings Tab — Caregiver (only Notifications + Account sections; Family and Children hidden).

    `## Group F — Sheets + States` (3 wireframes): 22) Event Detail bottom sheet (all 4 fields in order: event type selector → note field "Add a note (optional)" → time adjustment "Logged at [HH:mm]" → "Save details" button; title "Add details"; radius.lg top corners); 23) Child Switcher bottom sheet (title "Switch child", child rows with checkmark on active, 48dp rows, role=radio — REQ-031); 24) Loading state + Error state generic patterns (CircularProgressIndicator/ProgressView color.primary centered; error Body 16sp color.error with "Couldn't load data. Pull down to refresh.").

    Group D and Group E main-app wireframes must show the 4-tab bottom bar except the full-screen History Day-Detail (tab bar hidden, annotate why per REQ-035). The Settings (Admin) "Delete my data" row must be visually distinct (color.error) and annotated as reachable in two taps from the Settings tab (REQ-014).
  </action>
  <verify>
    <automated>[ "$(grep -c '^### ' docs/WIREFRAMES.md)" -ge 22 ] && grep -q 'Delete my data' docs/WIREFRAMES.md && grep -q 'Switch child' docs/WIREFRAMES.md && grep -q 'Save details' docs/WIREFRAMES.md && grep -q 'No events yet' docs/WIREFRAMES.md && grep -qE 'Group D|Group E|Group F' docs/WIREFRAMES.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### ' docs/WIREFRAMES.md` returns ≥ 22 (all 24 wireframes; allows minor combined-state headings)
    - File contains `## Group D`, `## Group E`, and `## Group F` headings
    - File contains `Delete my data` annotated with `color.error` and a two-taps-from-Settings reachability note (REQ-014)
    - File contains `Switch child`, `Save details`, `Add a note (optional)`, and `Logged at` (bottom-sheet fields)
    - File contains heatmap intensity references (`low`, `medium`, `high`) and a legend with `less` / `more`
    - File contains the Caregiver Settings wireframe noting Family and Children sections are hidden
    - History Day-Detail wireframe annotated as tab-bar-hidden full-screen flow (REQ-035)
    - `grep -c 'Ref: UI-SPEC' docs/WIREFRAMES.md` returns ≥ 22
  </acceptance_criteria>
  <done>Groups D–F (10 more wireframes) appended; docs/WIREFRAMES.md now covers all 24 screen/state variants with REQ-014 delete-flow visibility and role-differentiated Settings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-SPEC.md → WIREFRAMES.md | Approved screen/component intent crosses into per-screen layout specs engineers build UI from; omitted states or buried delete flow propagate compliance gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Repudiation | Consent screen spec omission | mitigate | Wireframe 9 must render the exact REQ-009 self-attestation copy; acceptance criteria greps for the verbatim string |
| T-04-07 | Repudiation / compliance | Delete flow buried in settings (REQ-014) | mitigate | Settings-Admin wireframe (20) must show "Delete my data" in color.error reachable within two taps; verify grep asserts presence |
| T-04-08 | Tampering | Missing screen states (Pitfall 2) | mitigate | 24-item Screen Inventory used as explicit checklist; `grep -c '^### '` gate requires ≥ 22 wireframe headings |
| T-04-09 | Information disclosure | Caregiver sees admin-only controls | mitigate | Wireframe 21 (Caregiver Settings) explicitly hides Family + Children sections (V4 Access Control); annotation documents the role gate |

No npm/pip/cargo installs in this plan — no supply-chain checkpoint required.
</threat_model>

<verification>
- `ls docs/WIREFRAMES.md` confirms the file exists
- `grep -c '^### ' docs/WIREFRAMES.md` ≥ 22 confirms all distinct screen/state wireframes (Pitfall 2 guard)
- `grep 'Delete my data'` + `grep 'parent or legal guardian'` confirm compliance-critical screens are specced
</verification>

<success_criteria>
docs/WIREFRAMES.md contains a lo-fi ASCII wireframe with implementation-grade annotations for every one of the 24 screen/state variants in the Screen Inventory, each cross-referenced to its 04-UI-SPEC.md component spec, satisfying ROADMAP Phase 4 Success Criterion 2 and surfacing the 4-tab bar (REQ-035) on every main-app screen.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Created:**
- `docs/WIREFRAMES.md` — Groups A–F, 24 ASCII wireframes (Auth+Org 6, Onboarding 4, Home+overlays 4, History 3, Progress+Settings 4, Sheets+States 3), each with token/a11y annotations and UI-SPEC Ref lines
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-ux-design/04-03-SUMMARY.md` when done.
</output>
