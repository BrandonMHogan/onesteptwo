---
phase: 04-ui-ux-design
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - .planning/phases/04-ui-ux-design/04-UI-SPEC.md
autonomous: true
requirements: [REQ-035]
must_haves:
  truths:
    - "All three output docs (DESIGN-TOKENS, SCREEN-FLOWS, WIREFRAMES) exist and pass the success-criteria grep checks"
    - "REQ-035 (4-tab navigation, active/inactive states) is verifiably documented across tokens and wireframes"
    - "The 04-UI-SPEC.md checker sign-off footer reads approved (not 'pending') so Phase 5 engineers know the spec is final"
  artifacts:
    - path: ".planning/phases/04-ui-ux-design/04-UI-SPEC.md"
      provides: "Approved design contract with final sign-off marker"
      contains: "Approval:"
  key_links:
    - from: ".planning/phases/04-ui-ux-design/04-UI-SPEC.md"
      to: "docs/DESIGN-TOKENS.md"
      via: "deliverables checklist satisfied"
      pattern: "DESIGN-TOKENS"
---

<objective>
Verify all three Phase 4 output documents satisfy the five ROADMAP Success Criteria, then finalize the stale `04-UI-SPEC.md` checker sign-off marker so Phase 5 engineers see an approved contract.

Purpose: STATE.md records the UI-SPEC as approved, but the in-file footer still reads `**Approval:** pending` (Pitfall 3) — a discrepancy that would confuse engineers into thinking the spec needs revision. This plan runs the phase-gate checklist and closes that gap after the three docs exist.
Output: a verification pass (no new doc) + updated 04-UI-SPEC.md sign-off footer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux-design/04-UI-SPEC.md
@docs/DESIGN-TOKENS.md
@docs/SCREEN-FLOWS.md
@docs/WIREFRAMES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Phase 4 success-criteria verification across all output docs</name>
  <files>docs/DESIGN-TOKENS.md, docs/SCREEN-FLOWS.md, docs/WIREFRAMES.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 Success Criteria 1–5, lines 116–122)
    - .planning/phases/04-ui-ux-design/04-RESEARCH.md (Phase Requirements → Test Map lines 622–628; SC grep commands)
    - docs/DESIGN-TOKENS.md
    - docs/SCREEN-FLOWS.md
    - docs/WIREFRAMES.md
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Component Inventory — to confirm SC-4: 12 components already specced)
  </read_first>
  <action>
    Run the success-criteria checklist against the three output docs and the existing UI-SPEC component inventory. For each criterion, run the mapped grep/test and confirm it passes; if any fails, STOP and report the specific gap (do not patch silently — the owning plan must be re-run):
    - SC-1 (screen flows): SCREEN-FLOWS.md contains ≥ 2 `flowchart`/`mermaid` blocks AND the Home Tab interaction sequence.
    - SC-2 (wireframes): WIREFRAMES.md has ≥ 22 `^### ` wireframe headings covering all 24 inventory items.
    - SC-3 (design tokens): DESIGN-TOKENS.md has ≥ 6 `^## ` sections (Color, Typography, Spacing, Corner Radius, Elevation, Motion).
    - SC-4 (component specs): 04-UI-SPEC.md §Component Inventory has 12 `^### [0-9]` component headings (already satisfied — confirm, do not duplicate into a new file).
    - REQ-035 (4-tab bar): tab labels Home/History/Progress/Settings appear in WIREFRAMES.md and tab active/inactive color tokens appear in DESIGN-TOKENS.md.
    Record the pass/fail result of each check in the plan SUMMARY.
  </action>
  <verify>
    <automated>[ "$(grep -cE '^## ' docs/DESIGN-TOKENS.md)" -ge 6 ] && [ "$(grep -c '^### ' docs/WIREFRAMES.md)" -ge 22 ] && [ "$(grep -c '```mermaid' docs/SCREEN-FLOWS.md)" -ge 2 ] && [ "$(grep -cE '^### [0-9]' .planning/phases/04-ui-ux-design/04-UI-SPEC.md)" -eq 12 ] && grep -q 'Settings' docs/WIREFRAMES.md && echo "ALL SC PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## ' docs/DESIGN-TOKENS.md` ≥ 6 (SC-3)
    - `grep -c '^### ' docs/WIREFRAMES.md` ≥ 22 (SC-2)
    - `grep -c '```mermaid' docs/SCREEN-FLOWS.md` ≥ 2 (SC-1) and `grep -q 'Home Tab Interaction Flow' docs/SCREEN-FLOWS.md` succeeds
    - `grep -cE '^### [0-9]' .planning/phases/04-ui-ux-design/04-UI-SPEC.md` returns exactly 12 (SC-4, no new component file created)
    - All four tab labels (Home, History, Progress, Settings) found in WIREFRAMES.md AND tab active/inactive color tokens found in DESIGN-TOKENS.md (REQ-035)
  </acceptance_criteria>
  <done>All five Phase 4 success criteria verified PASS against the three output docs and the existing UI-SPEC component inventory; any failure reported with the specific owning plan.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize 04-UI-SPEC.md checker sign-off footer</name>
  <files>.planning/phases/04-ui-ux-design/04-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Checker Sign-Off lines 720–729 — currently 6 unchecked boxes and `**Approval:** pending`)
    - .planning/STATE.md (line 6 `stopped_at: Phase 04 UI-SPEC approved` — confirms approval already granted)
    - .planning/phases/04-ui-ux-design/04-RESEARCH.md (Pitfall 3 lines 380–388 — the exact replacement marker `**Approval:** 2026-06-29 — all six dimensions PASS`)
  </read_first>
  <action>
    Edit the §Checker Sign-Off section at the bottom of 04-UI-SPEC.md. Mark all six dimension checkboxes as checked (`- [x]` for Copywriting, Visuals, Color, Typography, Spacing, Registry Safety) and replace the line `**Approval:** pending` with `**Approval:** 2026-06-29 — all six dimensions PASS` per Pitfall 3 / Open Question 2. Make no other changes to the file — the token values, component specs, and screen inventory are the canonical source of truth and must remain byte-identical.
  </action>
  <verify>
    <automated>grep -q '\*\*Approval:\*\* 2026-06-29 — all six dimensions PASS' .planning/phases/04-ui-ux-design/04-UI-SPEC.md && [ "$(grep -c '^- \[x\]' .planning/phases/04-ui-ux-design/04-UI-SPEC.md)" -ge 6 ] && ! grep -q '\*\*Approval:\*\* pending' .planning/phases/04-ui-ux-design/04-UI-SPEC.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 04-UI-SPEC.md contains `**Approval:** 2026-06-29 — all six dimensions PASS`
    - 04-UI-SPEC.md no longer contains `**Approval:** pending`
    - `grep -c '^- \[x\]'` over the Checker Sign-Off block returns ≥ 6 (all six dimensions checked)
    - No token table, component spec, or screen-inventory line in 04-UI-SPEC.md was altered (only the sign-off footer + checkboxes changed)
  </acceptance_criteria>
  <done>04-UI-SPEC.md sign-off footer reads approved with all six dimensions checked; the rest of the contract is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| STATE.md approval ↔ UI-SPEC.md footer | The recorded approval status crosses into the in-file marker engineers read; a stale "pending" marker misrepresents the contract's status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Repudiation | Stale UI-SPEC approval marker (Pitfall 3) | mitigate | Task 2 replaces `**Approval:** pending` with the dated PASS marker; verify grep asserts the old marker is gone |
| T-04-11 | Tampering | Accidental edit of canonical spec values during footer update | mitigate | Acceptance criteria restricts Task 2 to the sign-off footer + checkboxes only; reviewer confirms no token/component lines changed |

No npm/pip/cargo installs in this plan — no supply-chain checkpoint required.
</threat_model>

<verification>
- Task 1 success-criteria greps all return PASS (SC-1 through SC-4 + REQ-035)
- `grep '**Approval:** 2026-06-29'` confirms the sign-off is finalized
- `! grep '**Approval:** pending'` confirms the stale marker is removed
</verification>

<success_criteria>
All five ROADMAP Phase 4 success criteria verify PASS against the produced documentation, and 04-UI-SPEC.md presents a finalized, approved sign-off so Phase 5 engineers begin implementation from a contract with no ambiguity about its status.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Modified:**
- `.planning/phases/04-ui-ux-design/04-UI-SPEC.md` — §Checker Sign-Off: six dimension checkboxes checked, `**Approval:**` line dated and marked PASS (footer-only change)

**No new files** — this plan is verification + sign-off; SC-4 (component specs) is already satisfied by the existing 04-UI-SPEC.md §Component Inventory.
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-ux-design/04-04-SUMMARY.md` when done.
</output>
