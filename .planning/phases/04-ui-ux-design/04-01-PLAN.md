---
phase: 04-ui-ux-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DESIGN-TOKENS.md
autonomous: true
requirements: [REQ-035]
must_haves:
  truths:
    - "An engineer can open docs/DESIGN-TOKENS.md and find every color, typography, spacing, corner-radius, elevation, and motion token with both Compose and SwiftUI values"
    - "The bottom tab bar active and inactive color tokens (REQ-035) are documented"
    - "Every token value in DESIGN-TOKENS.md traces verbatim to a section in 04-UI-SPEC.md — no invented values"
  artifacts:
    - path: "docs/DESIGN-TOKENS.md"
      provides: "Standalone cross-platform design token reference"
      contains: "## Motion Tokens"
      min_lines: 90
  key_links:
    - from: "docs/DESIGN-TOKENS.md"
      to: ".planning/phases/04-ui-ux-design/04-UI-SPEC.md"
      via: "verbatim value extraction"
      pattern: "7E22CE"
---

<objective>
Produce `docs/DESIGN-TOKENS.md` — a standalone, engineer-facing design token reference extracted verbatim from the approved `04-UI-SPEC.md`. Phase 5+ engineers implement the Compose theme (`Color.kt`, `Type.kt`, `Theme.kt`) and SwiftUI `AppColors` directly from this file.

Purpose: Token values must live in one canonical, easy-to-find reference so engineers never guess a hex, duration, or font size. Drift between this file and the UI-SPEC is the primary risk (Pitfall 1).
Output: `docs/DESIGN-TOKENS.md` with six `##` token sections (Color, Typography, Spacing, Corner Radius, Elevation, Motion).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux-design/04-UI-SPEC.md
@.planning/phases/04-ui-ux-design/04-PATTERNS.md
@docs/06-auth.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/DESIGN-TOKENS.md with six token sections</name>
  <files>docs/DESIGN-TOKENS.md</files>
  <read_first>
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (source of truth — §Spacing Scale lines 40–62, §Typography lines 70–91, §Color lines 99–166, §Corner Radii lines 252–256, §Elevation lines 262–268, §Motion Tokens lines 275–297)
    - .planning/phases/04-ui-ux-design/04-PATTERNS.md (DESIGN-TOKENS.md analog rules — §`docs/DESIGN-TOKENS.md`, lines 21–77; header + table + heading conventions)
    - docs/06-auth.md (analog file — header blockquote, markdown table style, backtick code-value convention)
  </read_first>
  <action>
    Create docs/DESIGN-TOKENS.md. Open with a `# Design Tokens — OneStepTwo` H1 followed immediately by a `>` blockquote citing `.planning/phases/04-ui-ux-design/04-UI-SPEC.md` (approved 2026-06-29) as the source of truth and stating values must not be altered without updating UI-SPEC.md first (per 04-PATTERNS.md lines 34–40). No YAML front matter.

    Create exactly six `## ` sections in this order, each a markdown table reproducing UI-SPEC values verbatim:

    1. `## Color Tokens` — three subsections matching UI-SPEC §Color structure: `### Semantic Tokens` (the 14-row table from UI-SPEC lines 136–151: color.background, color.surface, color.surface-container, color.primary, color.on-primary, color.secondary, color.on-secondary, color.on-background, color.on-surface, color.outline, color.error, color.on-error, color.success, color.on-success — columns Token | Light | Dark | Usage); `### Primary Purple Scale` (primary-50 through primary-900 from UI-SPEC lines 121–132); `### Heatmap Intensity Colors` (Empty/Low/Medium/High from UI-SPEC lines 155–160). Include the reserved-accent usage note for color.primary (REQ-035 tab active state = color.primary; inactive = color.on-surface at 60% opacity).
    2. `## Typography Tokens` — the 6-level table from UI-SPEC lines 70–76: Display, Headline, Title, Body, Label, Caption with columns Role | Size | Weight | Line Height | Letter Spacing | Android TextStyle | iOS Font.
    3. `## Spacing Tokens` — the 7-token table from UI-SPEC lines 42–48 (space-1 through space-16) plus the tap-target / component-height exceptions table from UI-SPEC lines 54–61.
    4. `## Corner Radius Tokens` — radius.sm, radius.md, radius.lg, radius.pill from UI-SPEC lines 252–255.
    5. `## Elevation Tokens` — Flat, Raised, Overlay with Android and iOS columns from UI-SPEC lines 264–267.
    6. `## Motion Tokens` — the 5-row base table from UI-SPEC lines 277–281 (columns Token | Value | Android API | SwiftUI API) AND a `### Specific Animation Specs` subsection reproducing the 6-row interaction table from UI-SPEC lines 288–294 (tab switch, log button press, badge glow, toast enter, toast exit, bottom sheet) including the exact `tween(...)`, `spring(stiffness=..., dampingRatio=...)`, and `.spring(response:..., dampingFraction:...)` API values.

    Backtick-format all code-literal values (`tween(150)`, `Color(0xFF7E22CE)`, `.easeInOut(duration: 0.15)`) and hex values in the Color tables per 04-PATTERNS.md lines 64–69. Use `##` and `###` only — no `####`. Every value must be copied, not re-derived; do not introduce any hex, size, or duration absent from 04-UI-SPEC.md.
  </action>
  <verify>
    <automated>test -f docs/DESIGN-TOKENS.md && [ "$(grep -cE '^## ' docs/DESIGN-TOKENS.md)" -ge 6 ] && grep -q '7E22CE' docs/DESIGN-TOKENS.md && grep -q 'tween(150)' docs/DESIGN-TOKENS.md && grep -q 'space-1' docs/DESIGN-TOKENS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^## ' docs/DESIGN-TOKENS.md` returns ≥ 6 (Color, Typography, Spacing, Corner Radius, Elevation, Motion)
    - `grep -c 'color\.' docs/DESIGN-TOKENS.md` returns ≥ 14 (all semantic tokens present)
    - File contains `7E22CE` (primary light), `D8B4FE` (primary dark), and `0E0A14` (background dark) — confirms color values transcribed
    - File contains `tween(150)`, `tween(300)`, `tween(400)`, and `spring(stiffness=300f, dampingRatio=0.6f)` — confirms motion API values transcribed
    - File contains `radius.pill` and `radius.lg`; `grep -c 'radius\.' docs/DESIGN-TOKENS.md` returns ≥ 4
    - File contains the strings `space-1`, `space-16`, and `48dp` (Android min tap target exception)
    - `grep -c '^#### ' docs/DESIGN-TOKENS.md` returns 0 (no fourth-level headings)
  </acceptance_criteria>
  <done>docs/DESIGN-TOKENS.md exists with six token categories, all values verbatim from 04-UI-SPEC.md, both Compose and SwiftUI columns present where applicable.</done>
</task>

<task type="auto">
  <name>Task 2: Diff-verify token values against 04-UI-SPEC.md</name>
  <files>docs/DESIGN-TOKENS.md</files>
  <read_first>
    - docs/DESIGN-TOKENS.md (the file just written)
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Color, §Typography, §Motion — to confirm no value drift)
  </read_first>
  <action>
    Cross-check every hex value, typography size, duration, and corner-radius value in docs/DESIGN-TOKENS.md against its source row in 04-UI-SPEC.md. For each of the 9 primary-scale hexes (primary-50=#FAF5FF through primary-900=#581C87), the 14 semantic color rows, the 5 motion duration/easing rows, and the 4 corner radii, confirm the DESIGN-TOKENS.md value is byte-identical to the UI-SPEC source. Fix any transposed digit or mismatched value found. Confirm no hex value appears in DESIGN-TOKENS.md that is absent from 04-UI-SPEC.md (the warning sign for Pitfall 1).
  </action>
  <verify>
    <automated>for hex in FAF5FF F3E8FF E9D5FF D8B4FE C084FC A855F7 9333EA 7E22CE 6B21A8 581C87; do grep -q "$hex" docs/DESIGN-TOKENS.md && grep -q "$hex" .planning/phases/04-ui-ux-design/04-UI-SPEC.md || { echo "MISMATCH $hex"; exit 1; }; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Every primary-scale hex (FAF5FF, F3E8FF, E9D5FF, D8B4FE, C084FC, A855F7, 9333EA, 7E22CE, 6B21A8, 581C87) present in BOTH docs/DESIGN-TOKENS.md and 04-UI-SPEC.md
    - No hex of form `#[0-9A-F]{6}` exists in DESIGN-TOKENS.md that is not also present in 04-UI-SPEC.md
    - Typography sizes 28, 24, 20, 16, 14, 12 each appear in the Typography section
    - Motion durations 150ms, 300ms, 400ms each appear in the Motion section
  </acceptance_criteria>
  <done>All token values confirmed byte-identical to 04-UI-SPEC.md; no orphan values introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-SPEC.md → DESIGN-TOKENS.md | Approved spec intent crosses into a derived reference engineers build from; mis-transcription propagates to production UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Token value transcription | mitigate | Task 2 diffs every hex/size/duration against 04-UI-SPEC.md; orphan-hex check fails the build if any value lacks a UI-SPEC source |
| T-04-02 | Information disclosure | n/a — no data processed | accept | Phase 4 produces only markdown documentation; no PII, secrets, or runtime endpoints involved |

No npm/pip/cargo installs in this plan — no T-04-SC supply-chain checkpoint required.
</threat_model>

<verification>
- `ls docs/DESIGN-TOKENS.md` confirms the file exists
- `grep -cE '^## ' docs/DESIGN-TOKENS.md` ≥ 6 confirms all six token categories
- Task 2 hex-parity loop confirms zero drift from the approved spec
</verification>

<success_criteria>
docs/DESIGN-TOKENS.md documents all six token categories (Color, Typography, Spacing, Corner Radius, Elevation, Motion) with both Jetpack Compose and SwiftUI values, every value traceable verbatim to 04-UI-SPEC.md, satisfying ROADMAP Phase 4 Success Criterion 3.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Created:**
- `docs/DESIGN-TOKENS.md` — six token-category sections (Color incl. semantic + primary scale + heatmap, Typography, Spacing, Corner Radius, Elevation, Motion incl. animation specs)
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-ux-design/04-01-SUMMARY.md` when done.
</output>
