---
phase: 04-ui-ux-design
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCREEN-FLOWS.md
autonomous: true
requirements: [REQ-035]
must_haves:
  truths:
    - "An engineer can trace every navigation path from app launch through auth, org-picker, onboarding wizard, and into the 4-tab main app"
    - "The log button → toast → bottom sheet in-screen interaction sequence is documented as a numbered flow (not omitted)"
    - "Tab-bar visibility rules (hidden during onboarding and full-screen detail flows, REQ-035) are stated"
    - "Platform-specific back-navigation exception (D-10) is documented"
  artifacts:
    - path: "docs/SCREEN-FLOWS.md"
      provides: "Navigation diagrams + interaction flow reference"
      contains: "Home Tab Interaction Flow"
      min_lines: 70
  key_links:
    - from: "docs/SCREEN-FLOWS.md"
      to: ".planning/phases/04-ui-ux-design/04-UI-SPEC.md"
      via: "navigation patterns transcription"
      pattern: "Wizard"
---

<objective>
Produce `docs/SCREEN-FLOWS.md` — the navigation reference covering every user path plus the in-screen log→toast→sheet interaction chain. Engineers use this to wire NavigationStack / NavHost routes and the tab shell in Phase 5.

Purpose: ROADMAP Phase 4 Success Criterion 1 requires a screen-flow diagram covering every path including the one-tap-log → add-details-later flow. The routing-only diagram easily omits the in-Home-Tab interaction sub-flow (Pitfall 4) — this plan forces it in.
Output: `docs/SCREEN-FLOWS.md` with auth+onboarding flow, main-app navigation flow, the Home Tab interaction numbered sequence, and platform-specific navigation notes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-ux-design/04-UI-SPEC.md
@.planning/phases/04-ui-ux-design/04-PATTERNS.md
@docs/03-system-architecture.md
@docs/07-sync-and-notifications.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/SCREEN-FLOWS.md navigation diagrams (auth/onboarding + main app)</name>
  <files>docs/SCREEN-FLOWS.md</files>
  <read_first>
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Screen Inventory lines 501–597, §Navigation Patterns lines 600–642)
    - .planning/phases/04-ui-ux-design/04-PATTERNS.md (SCREEN-FLOWS.md analog rules — lines 81–149; header, section headings, Mermaid-vs-ASCII guidance, blockquote callout)
    - .planning/phases/04-ui-ux-design/04-RESEARCH.md (Pattern 2 Mermaid example lines 216–246; Code Examples lines 459–490)
    - docs/03-system-architecture.md (analog file — header blockquote + fenced ASCII diagram convention)
    - .planning/REQUIREMENTS.md (REQ-035 tab visibility, REQ-018 multi-org picker, REQ-036 wizard step order)
  </read_first>
  <action>
    Create docs/SCREEN-FLOWS.md. Open with `# Screen Flows — OneStepTwo` H1 then a `>` blockquote citing 04-UI-SPEC.md §Screen Inventory + §Navigation Patterns as source and `> Last updated: 2026-06-29`. No YAML front matter.

    Add `## Auth + Onboarding Flow` containing a ` ```mermaid ` `flowchart TD` diagram covering: App Launch → Clerk-initialized check (loading → full-screen spinner) → signed-in check → Sign In / Sign Up → OTP verification → multi-org check → Org Picker (REQ-018) → admin-first-time check → Wizard Step 2 (Family Name) → Step 3 (Child Details) → Step 4 (Consent — node must be labelled to reflect the self-attestation gate, button disabled until checked) → Step 5 (Success) → Home Tab; and the caregiver-invite branch that skips the wizard straight to Home Tab. Immediately below the Mermaid block, add a numbered plain-text fallback list of the same path (per Assumption A1 / Open Question 1 — guards against viewers without Mermaid support).

    Add `## Main App Navigation` containing a `flowchart` diagram for the 4-tab shell (REQ-035): Home, History, Progress, Settings tabs, plus History → History Day-Detail push (back returns to History), child-name tap → Child Switcher sheet, and the role split for the Settings tab (Admin: 4 sections / Caregiver: 2 sections). Annotate which destinations hide the tab bar (History Day-Detail, onboarding wizard) per REQ-035.

    Use `##` for sections, `###` only if needed; no `####`. Fenced Mermaid blocks use ` ```mermaid `; fenced ASCII fallbacks use a plain ` ``` ` block (40–42 char width) per 04-PATTERNS.md lines 246–251.
  </action>
  <verify>
    <automated>test -f docs/SCREEN-FLOWS.md && [ "$(grep -c '```mermaid' docs/SCREEN-FLOWS.md)" -ge 2 ] && grep -q 'Org Picker' docs/SCREEN-FLOWS.md && grep -qi 'consent' docs/SCREEN-FLOWS.md && grep -q 'Day-Detail' docs/SCREEN-FLOWS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '```mermaid' docs/SCREEN-FLOWS.md` returns ≥ 2 (auth/onboarding + main-app diagrams)
    - File contains nodes/labels for `Sign In`, `Org Picker`, `Wizard`/`Family Name`, `Consent`, and `Home Tab`
    - File contains all four tab names: `Home`, `History`, `Progress`, `Settings`
    - File contains `Day-Detail` and a statement that the tab bar is hidden during it (REQ-035)
    - File contains a plain-text numbered fallback list beneath at least the auth Mermaid diagram (Mermaid-unsupported viewers)
    - `grep -c '^#### ' docs/SCREEN-FLOWS.md` returns 0
  </acceptance_criteria>
  <done>docs/SCREEN-FLOWS.md exists with two navigation diagrams covering auth/onboarding and the 4-tab main app, including the consent gate node and tab-bar visibility rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add Home Tab interaction flow + platform-specific navigation notes</name>
  <files>docs/SCREEN-FLOWS.md</files>
  <read_first>
    - docs/SCREEN-FLOWS.md (the file from Task 1)
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md (§Log Button → Toast → Bottom Sheet Flow lines 615–623; §Navigation Patterns Back Navigation lines 602–606; Tab Switch lines 608–613; Onboarding Wizard Navigation lines 631–636; Auth → Main App Transition lines 638–642)
    - .planning/phases/04-ui-ux-design/04-PATTERNS.md (numbered-sequence pattern lines 130–139; blockquote callout pattern lines 141–149)
    - docs/07-sync-and-notifications.md (analog — numbered sequence style)
  </read_first>
  <action>
    Append to docs/SCREEN-FLOWS.md a `## Home Tab Interaction Flow (Log → Toast → Sheet)` section. Document the 7-step chain from UI-SPEC lines 615–623 as a numbered list: (1) tap Log button; (2) SQLDelight write fires immediately, no network dependency; (3) haptic on release + button scale 0.95→1.0 spring; (4) toast slides up from above tab bar (200ms enter); (5) tap event-type chip → event_type written to SQLDelight → toast exits (150ms); (6) tap "add details" in toast OR a pending-details card later → bottom sheet opens (platform default timing); (7) fill fields + Save details → sheet dismisses, event updated in SQLDelight. Optionally add a small ` ```mermaid ` sub-diagram of this chain.

    Append a `## Platform-Specific Navigation Notes` section using `>` blockquote callouts (per 04-PATTERNS.md lines 141–149): the D-10 back-navigation exception (iOS NavigationStack swipe-from-left-edge; Android system predictive back gesture; no custom back button); the D-32 tab-switch cross-dissolve (120ms, state preserved across tab switches); the onboarding wizard linearity (no back from consent step 4; Step 5 replaces nav stack root with the tab shell); and the auth→main-app transition (replace nav stack root, back stack cleared, matches Phase 3 `popUpTo(0) { inclusive = true }`).
  </action>
  <verify>
    <automated>grep -q 'Home Tab Interaction Flow' docs/SCREEN-FLOWS.md && grep -q 'Platform-Specific Navigation Notes' docs/SCREEN-FLOWS.md && grep -qi 'SQLDelight' docs/SCREEN-FLOWS.md && grep -qi 'predictive back' docs/SCREEN-FLOWS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File contains `## Home Tab Interaction Flow (Log → Toast → Sheet)`
    - The interaction section is a numbered list with ≥ 7 steps referencing `SQLDelight`, `toast`, and `Save details`
    - File contains `## Platform-Specific Navigation Notes`
    - Platform notes mention `predictive back` (Android) and `NavigationStack` swipe (iOS) — the D-10 exception
    - Platform notes state the consent step (step 4) has no back navigation and Step 5 replaces the nav stack root
  </acceptance_criteria>
  <done>docs/SCREEN-FLOWS.md documents the log→toast→sheet interaction chain as a numbered sequence and records all platform-specific navigation exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-SPEC.md → SCREEN-FLOWS.md | Approved navigation + consent-gate intent crosses into the flow reference engineers wire routes from |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation | Consent-gate flow omission | mitigate | Auth/onboarding Mermaid diagram MUST include the Step-4 consent node before Step-5/Home; acceptance criteria grep for a `consent` node |
| T-04-04 | Tampering | Interaction-flow omission (log→toast→sheet) | mitigate | Task 2 mandates the 7-step numbered interaction section; verify grep asserts `Home Tab Interaction Flow` present |
| T-04-05 | Denial of service | Mermaid renderer unsupported in viewer | accept | Plain-text numbered fallback added beneath diagrams (Assumption A1); raw syntax remains human-readable |

No npm/pip/cargo installs in this plan — no supply-chain checkpoint required.
</threat_model>

<verification>
- `ls docs/SCREEN-FLOWS.md` confirms the file exists
- `grep -c '```mermaid' docs/SCREEN-FLOWS.md` ≥ 2 confirms both navigation diagrams
- `grep 'Home Tab Interaction Flow'` confirms the interaction sub-flow is not omitted (Pitfall 4)
</verification>

<success_criteria>
docs/SCREEN-FLOWS.md covers every navigation path in 04-UI-SPEC.md §Screen Inventory — auth, org-picker, onboarding wizard (with consent gate), 4-tab main app, history drill-down — plus the log→toast→sheet interaction chain and platform-specific navigation exceptions, satisfying ROADMAP Phase 4 Success Criterion 1 and the navigation half of Success Criterion 5.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Created:**
- `docs/SCREEN-FLOWS.md` — sections: Auth + Onboarding Flow (Mermaid + text fallback), Main App Navigation (Mermaid), Home Tab Interaction Flow (numbered), Platform-Specific Navigation Notes
</artifacts_produced>

<output>
Create `.planning/phases/04-ui-ux-design/04-02-SUMMARY.md` when done.
</output>
