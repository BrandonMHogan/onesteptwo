---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - local.properties
  - gradlew
  - gradlew.bat
  - gradle/wrapper/gradle-wrapper.properties
  - gradle/wrapper/gradle-wrapper.jar
  - gradle/libs.versions.toml
  - settings.gradle.kts
  - build.gradle.kts
  - gradle.properties
  - shared/build.gradle.kts
  - shared/src/commonMain/sqldelight/migrations/1.sqm
  - androidApp/build.gradle.kts
  - androidApp/src/main/AndroidManifest.xml
  - androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt
  - androidApp/src/debug/google-services.json
autonomous: true
requirements: [REQ-025]
must_haves:
  truths:
    - "./gradlew --version reports Gradle 8.10.2 running on JVM 21"
    - "The SQLDelight migration file 1.sqm exists at shared/src/commonMain/sqldelight/migrations/ from day one (REQ-025)"
    - "The SQLDelight migration verification task succeeds for the :shared module"
    - "./gradlew :androidApp:assembleDebug produces a debug APK"
    - "All Gradle module versions are resolved through gradle/libs.versions.toml (no hardcoded version strings, D-07)"
  artifacts:
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Pinned Gradle 8.10.2 wrapper (Kotlin 2.0.21 forbids Gradle 9.x)"
      contains: "gradle-8.10.2-bin.zip"
    - path: "gradle/libs.versions.toml"
      provides: "Single version catalog for all modules (D-07)"
      contains: "kotlin = \"2.0.21\""
    - path: "settings.gradle.kts"
      provides: "Includes :shared and :androidApp only (D-08; iosApp is not a Gradle module)"
      contains: "include(\":shared\")"
    - path: "shared/build.gradle.kts"
      provides: "KMP module with SQLDelight + verifyMigrations enforcing REQ-025"
      contains: "verifyMigrations.set(true)"
    - path: "shared/src/commonMain/sqldelight/migrations/1.sqm"
      provides: "Empty placeholder migration required from day one (REQ-025)"
      min_lines: 1
    - path: "androidApp/build.gradle.kts"
      provides: "Jetpack Compose app module (minSdk 29, compileSdk 36)"
      contains: "applicationId = \"com.onesteptwo.android\""
    - path: "androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt"
      provides: "Minimal Compose entrypoint so assembleDebug links"
      contains: "class MainActivity"
  key_links:
    - from: "androidApp/build.gradle.kts"
      to: "shared/build.gradle.kts"
      via: "implementation(project(\":shared\"))"
      pattern: "project\\(\":shared\"\\)"
    - from: "shared/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "alias(libs.plugins.sqldelight) and libs.sqldelight.* references"
      pattern: "libs\\.plugins\\.sqldelight"
---

<objective>
Scaffold the Kotlin Multiplatform monorepo and its local toolchain: install JDK 21 and the Android SDK, pin the Gradle 8.10.2 wrapper, create the `libs.versions.toml` version catalog, the root + `:shared` (KMP) + `:androidApp` (Compose) build files, the day-one SQLDelight `1.sqm` migration, and a minimal Compose `MainActivity` so the Android app assembles.

Purpose: Deliver the mobile build skeleton that compiles clean and enforces REQ-025 (SQLDelight `.sqm` migrations from version 1 with no gaps). No application/UI logic — scaffolding only.
Output: A repo where `./gradlew :androidApp:assembleDebug` succeeds and the SQLDelight migration is validated at build time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-PATTERNS.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
</context>

<artifacts_this_phase_produces>
Symbols and files this plan creates (new — exclude from drift verification):
- Gradle version catalog `gradle/libs.versions.toml` (versions: kotlin 2.0.21, agp 8.7.3, sqldelight 2.3.2, skie 0.10.12, compose-bom; plugin + library aliases)
- Gradle build scripts: root `build.gradle.kts`, `settings.gradle.kts`, `shared/build.gradle.kts`, `androidApp/build.gradle.kts`, `gradle.properties`
- SQLDelight database `OneStepTwoDatabase` (package `com.onesteptwo.db`) and migration `1.sqm`
- Android module `com.onesteptwo.android` with `MainActivity`
- Gradle 8.10.2 wrapper (`gradlew`, `gradlew.bat`, wrapper jar + properties)
- `.gitignore`, `local.properties` (gitignored), committed debug `google-services.json` placeholder
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Install toolchain (JDK 21, Android SDK, Gradle 8.10.2 wrapper) and repo hygiene</name>
  <files>.gitignore, local.properties, gradlew, gradlew.bat, gradle/wrapper/gradle-wrapper.properties, gradle/wrapper/gradle-wrapper.jar, gradle.properties</files>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (§Environment Availability — Java 21 and Android SDK are NOT installed; Pitfalls 1 and 2; §State of the Art — Java 21 not 17)
    - .planning/phases/01-foundation-infrastructure/01-PATTERNS.md (§No Analog Found — gradlew, gradle-wrapper.properties distributionUrl, .gitignore, local.properties)
    - .planning/PROJECT.md (locked: Kotlin 2.0.21 pinned, Java toolchain 21, minSdk 29, compileSdk/targetSdk 36)
  </read_first>
  <action>
    Install Temurin JDK 21: `brew install --cask temurin@21`. Confirm `java -version` reports a 21.x runtime; export/record `JAVA_HOME` for the Temurin 21 install. Java is currently absent (verified) and is required before any Gradle task (Pitfall 2).
    Install the Android command-line tools and SDK packages: `brew install --cask android-commandlinetools`, then use `sdkmanager` to install `platform-tools`, `platforms;android-36`, and `build-tools;36.0.0`, and run `sdkmanager --licenses` accepting all (yes). compileSdk/targetSdk are 36 and minSdk is 29 (PROJECT.md).
    Install Gradle once only to generate the wrapper: `brew install gradle` (Gradle is absent locally). Then generate the pinned wrapper: `gradle wrapper --gradle-version 8.10.2 --distribution-type bin`. Confirm `gradle/wrapper/gradle-wrapper.properties` `distributionUrl` ends in `gradle-8.10.2-bin.zip`. Gradle 9.x must NOT be used — Kotlin 2.0.21 only supports up to 8.10.x (Pitfall 1).
    Create `local.properties` with `sdk.dir=` pointing at the installed Android SDK location (the directory `sdkmanager` populated). This file MUST be gitignored.
    Create `gradle.properties` with `org.gradle.jvmargs=-Xmx4096M`, `android.useAndroidX=true`, and `kotlin.code.style=official`.
    Create `.gitignore` covering: `.gradle/`, `build/`, `local.properties`, `*.iml`, `.idea/`, `.DS_Store`, `androidApp/src/release/google-services.json` (production Firebase config must never be committed — Pitfall 8 / security T-02-01), and standard Kotlin/Android/Go ignores. Do NOT ignore `backend/internal/api/generated.go` (D-03 requires it committed) and do NOT ignore `androidApp/src/debug/google-services.json` (staging config is committed).
  </action>
  <verify>
    <automated>java -version 2>&1 | grep -q '21' && grep -q 'gradle-8.10.2-bin.zip' gradle/wrapper/gradle-wrapper.properties && ./gradlew --version | grep -q '8.10.2' && grep -q 'local.properties' .gitignore && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `java -version` output contains `21`
    - `gradle/wrapper/gradle-wrapper.properties` `distributionUrl` contains `gradle-8.10.2-bin.zip`
    - `./gradlew --version` reports `Gradle 8.10.2` and a JVM 21.x
    - `local.properties` exists with a `sdk.dir=` line and is listed in `.gitignore`
    - `.gitignore` contains `local.properties` and `androidApp/src/release/google-services.json`
    - `.gitignore` does NOT contain `generated.go` and does NOT contain `src/debug/google-services.json`
  </acceptance_criteria>
  <done>JDK 21, Android SDK 36, and the pinned Gradle 8.10.2 wrapper are installed and runnable; repo hygiene files are in place with secrets excluded.</done>
</task>

<task type="auto">
  <name>Task 2: Version catalog, root + :shared KMP build, SQLDelight 1.sqm</name>
  <files>gradle/libs.versions.toml, settings.gradle.kts, build.gradle.kts, shared/build.gradle.kts, shared/src/commonMain/sqldelight/migrations/1.sqm</files>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-PATTERNS.md (sections: `gradle/libs.versions.toml`, `settings.gradle.kts`, `build.gradle.kts`, `shared/build.gradle.kts`, `1.sqm` — canonical contents)
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (§Pattern 3 SQLDelight KMP; §Pattern 5 catalog; Pitfall 3 — 1.sqm semantics; Anti-Patterns — no BEGIN/END TRANSACTION in .sqm)
    - .planning/PROJECT.md (locked: SKIE only, no KMP-NativeCoroutines; SQLDelight .sqm; Kotlin 2.0.21)
  </read_first>
  <action>
    Create `gradle/libs.versions.toml` exactly as PATTERNS §version catalog: versions kotlin=2.0.21, agp=8.7.3, sqldelight=2.3.2, skie=0.10.12, compose-bom; the sqldelight + compose + activity library aliases; and plugin aliases for kotlin-multiplatform, kotlin-android, android-application, sqldelight, skie, google-services (4.4.2). All version management lives here (D-07) — no hardcoded versions in any build file.
    Create `settings.gradle.kts` with `pluginManagement` and `dependencyResolutionManagement` repositories (google, mavenCentral, gradlePluginPortal), `rootProject.name = "onesteptwo"`, and `include(":shared")` + `include(":androidApp")` — only those two modules (D-08; `iosApp/` is an Xcode project, not a Gradle module).
    Create the root `build.gradle.kts` declaring all plugins with `apply false` via the catalog aliases (kotlin-multiplatform, kotlin-android, android-application, sqldelight, skie, google-services).
    Create `shared/build.gradle.kts` per PATTERNS §Pattern 3: apply kotlin-multiplatform + sqldelight; declare `androidTarget()` and the three iOS targets (iosX64, iosArm64, iosSimulatorArm64) each producing a framework `baseName = "shared"`; `jvmToolchain(21)`; commonMain depends on `libs.sqldelight.runtime`, androidMain on `libs.sqldelight.android.driver`, iosMain on `libs.sqldelight.native.driver`. Add the required KMP Android config block to the module: an `android { namespace = "com.onesteptwo.shared"; compileSdk = 36; defaultConfig { minSdk = 29 } }` block (androidTarget requires namespace + compileSdk to configure). Add the `sqldelight { databases { create("OneStepTwoDatabase") { packageName.set("com.onesteptwo.db"); verifyMigrations.set(true) } } }` block — `verifyMigrations` enforces REQ-025 at build time.
    Create `shared/src/commonMain/sqldelight/migrations/1.sqm` as a comment-only placeholder (no schema tables yet — Phase 2 adds `.sq` files). Do NOT wrap it in BEGIN/END TRANSACTION (Anti-Pattern — SQLDelight crashes). The file must exist from day one (REQ-025, Pitfall 3).
    Run the SQLDelight generation/verification task for the :shared module to prove the catalog resolves, the plugin loads, and the migration is valid.
  </action>
  <verify>
    <automated>test -f shared/src/commonMain/sqldelight/migrations/1.sqm && grep -q 'verifyMigrations.set(true)' shared/build.gradle.kts && grep -q 'kotlin = "2.0.21"' gradle/libs.versions.toml && ./gradlew :shared:generateCommonMainOneStepTwoDatabaseInterface --no-daemon && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contains `kotlin = "2.0.21"`, `sqldelight = "2.3.2"`, `skie = "0.10.12"`, `agp = "8.7.3"`
    - `settings.gradle.kts` contains `include(":shared")` and `include(":androidApp")` and references no other modules
    - `shared/build.gradle.kts` contains `verifyMigrations.set(true)`, `packageName.set("com.onesteptwo.db")`, `jvmToolchain(21)`, and an `android { ... compileSdk = 36 ... }` block with `minSdk = 29`
    - `shared/build.gradle.kts` references plugins only via `alias(libs.plugins.*)` and libraries only via `libs.*` (no literal version strings)
    - `shared/src/commonMain/sqldelight/migrations/1.sqm` exists and contains no `BEGIN`/`COMMIT`/`END TRANSACTION`
    - `./gradlew :shared:generateCommonMainOneStepTwoDatabaseInterface --no-daemon` exits 0
  </acceptance_criteria>
  <done>The version catalog, root build, and :shared KMP module are in place; SQLDelight resolves and the 1.sqm placeholder validates at build time.</done>
</task>

<task type="auto">
  <name>Task 3: :androidApp Compose module, MainActivity, debug Firebase placeholder, assembleDebug</name>
  <files>androidApp/build.gradle.kts, androidApp/src/main/AndroidManifest.xml, androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt, androidApp/src/debug/google-services.json</files>
  <read_first>
    - .planning/phases/01-foundation-infrastructure/01-PATTERNS.md (section: `androidApp/build.gradle.kts` — full Compose module pattern, including buildTypes, compileOptions, compose buildFeature, Compose BOM deps; Firebase json placement notes)
    - .planning/phases/01-foundation-infrastructure/01-RESEARCH.md (Pitfall 8 — google-services.json path; §Open Questions #2 — commit staging debug config)
    - shared/build.gradle.kts (created in Task 2 — androidApp depends on :shared)
    - .planning/PROJECT.md (locked: applicationId com.onesteptwo.android, minSdk 29, compileSdk/targetSdk 36, Jetpack Compose)
  </read_first>
  <action>
    Create `androidApp/build.gradle.kts` per PATTERNS: apply android-application + kotlin-android + google-services via catalog aliases; `android { namespace = "com.onesteptwo.android"; compileSdk = 36; defaultConfig { applicationId = "com.onesteptwo.android"; minSdk = 29; targetSdk = 36; versionCode = 1; versionName = "0.1.0" } }`; `buildTypes` with a `debug` (staging) and a `release` (isMinifyEnabled = true, proguard files); `compileOptions` source/target = JavaVersion.VERSION_21; `buildFeatures { compose = true }`; `kotlin { jvmToolchain(21) }`. Dependencies: `implementation(project(":shared"))`, the Compose BOM platform, compose-ui, compose-material3, activity-compose — all via `libs.*`.
    Create `androidApp/src/main/AndroidManifest.xml` declaring an `<application>` with a launcher `<activity android:name=".MainActivity" android:exported="true">` carrying the MAIN/LAUNCHER intent filter.
    Create `androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt`: a minimal `class MainActivity : ComponentActivity()` whose `onCreate` calls `setContent { }` rendering a single Material3 placeholder composable (e.g. a `Text("OneStepTwo")`). No app logic — this exists only so `assembleDebug` links a real entrypoint.
    Create `androidApp/src/debug/google-services.json` as a STRUCTURALLY VALID placeholder so the google-services plugin and `assembleDebug` succeed before the real Firebase staging project exists (provisioned in Plan 01-04). It must contain a `project_info` block and a `client` entry whose `android_client_info.package_name` equals `com.onesteptwo.android`. This placeholder is committed (staging config is non-sensitive, Open Question #2); Plan 01-04 replaces it with the real staging project's file. The release google-services.json is NOT created here and is gitignored.
    Run `./gradlew :androidApp:assembleDebug --no-daemon` and confirm a debug APK is produced.
  </action>
  <verify>
    <automated>grep -q 'applicationId = "com.onesteptwo.android"' androidApp/build.gradle.kts && grep -q 'class MainActivity' androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt && grep -q 'com.onesteptwo.android' androidApp/src/debug/google-services.json && ./gradlew :androidApp:assembleDebug --no-daemon && ls androidApp/build/outputs/apk/debug/*.apk && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `androidApp/build.gradle.kts` contains `applicationId = "com.onesteptwo.android"`, `minSdk = 29`, `compileSdk = 36`, `compose = true`, and `implementation(project(":shared"))`
    - `androidApp/src/main/AndroidManifest.xml` declares `MainActivity` with a MAIN/LAUNCHER intent filter and `android:exported="true"`
    - `androidApp/src/main/kotlin/com/onesteptwo/android/MainActivity.kt` contains `class MainActivity` extending `ComponentActivity` and a `setContent` call
    - `androidApp/src/debug/google-services.json` is valid JSON whose android client `package_name` is `com.onesteptwo.android`
    - `./gradlew :androidApp:assembleDebug --no-daemon` exits 0 and a `.apk` exists under `androidApp/build/outputs/apk/debug/`
  </acceptance_criteria>
  <done>The Compose app module assembles a debug APK against the :shared module using a committed staging Firebase placeholder.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo → git history | Build config committed; production Firebase config and signing material must never enter git |
| Developer machine → Maven Central / Google Maven | Gradle plugins and libraries downloaded; integrity via Gradle wrapper + dependency coordinates |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | high | google-services.json (production) / signing keys | mitigate | `.gitignore` excludes `androidApp/src/release/google-services.json`; only the non-sensitive staging debug placeholder is committed; production Firebase config injected via CI secret in later plans |
| T-02-02 | Tampering | medium | Gradle wrapper distribution | mitigate | Wrapper pinned to `gradle-8.10.2-bin.zip` from gradle.org; wrapper jar committed; reproducible build version (also avoids the Kotlin 2.0.21 + Gradle 9.x break) |
| T-02-SC | Tampering | medium | Gradle plugin installs (sqldelight, skie, AGP, kotlin) | mitigate | All plugins verified OK/Approved in RESEARCH §Package Legitimacy Audit (Cash App, Touchlab, Google/JetBrains official); versions pinned in libs.versions.toml. No [ASSUMED]/[SUS]/[SLOP] legitimacy verdicts — no blocking checkpoint required. slopcheck on libs.versions.toml diff during review |
| T-02-03 | Information Disclosure | low | local.properties (sdk.dir) | mitigate | local.properties gitignored; contains only a local filesystem path, no secret |
</threat_model>

<verification>
- `./gradlew --version` reports Gradle 8.10.2 on JVM 21
- `./gradlew :shared:generateCommonMainOneStepTwoDatabaseInterface --no-daemon` exits 0 (REQ-025 1.sqm valid)
- `./gradlew :androidApp:assembleDebug --no-daemon` exits 0 with a debug APK produced
- `git check-ignore androidApp/src/release/google-services.json local.properties` lists both as ignored
- `git check-ignore backend/internal/api/generated.go` returns nothing (it must be committable)
</verification>

<success_criteria>
- JDK 21, Android SDK 36, and Gradle 8.10.2 are installed and the wrapper runs
- `1.sqm` exists from day one and SQLDelight verifies it (REQ-025)
- The KMP `:shared` module and the Compose `:androidApp` module both build; `assembleDebug` yields an APK
- Every version is sourced from `libs.versions.toml`; no secrets are committed
</success_criteria>

<output>
Create `.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md` when done.
</output>
