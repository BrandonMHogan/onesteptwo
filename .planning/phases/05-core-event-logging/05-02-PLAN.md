---
phase: 05-core-event-logging
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - shared/src/commonMain/sqldelight/com/onesteptwo/db/PottyEvents.sq
  - shared/src/commonMain/kotlin/com/onesteptwo/data/NotificationPreferencesRepository.kt
  - shared/src/commonMain/sqldelight/com/onesteptwo/db/NotificationPreferences.sq
  - shared/src/commonMain/kotlin/com/onesteptwo/api/ApiClient.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/HeatmapView.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/HistoryScreen.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/HistoryViewModel.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/DayDetailScreen.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/home/EventDetailSheet.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/SettingsScreen.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/SettingsViewModel.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/ChildManagementScreen.kt
  - androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/NotificationPreferenceRow.kt
  - api/openapi.yaml
  - backend/internal/api/notification_preferences_handler.go
  - backend/internal/api/children_handler.go
autonomous: true
requirements: [REQ-007, REQ-014, REQ-017, REQ-022, REQ-033]
must_haves:
  truths:
    - "The History tab shows a rolling heatmap (grows from week 1, caps at 12 weeks) with correct intensity levels; tapping a non-empty cell opens a day-detail list"
    - "Every event card in History Day-Detail is tappable and reopens the Event Detail sheet pre-filled; the sheet has a working Delete event destructive action"
    - "Settings (admin) shows family members + invite (reusing the existing InviteCaregiverScreen), child management with real add/edit/remove, a real per-child notification toggle backed by a new Go endpoint, account info, sign out, and Delete my data reachable in 2 taps"
    - "Settings (caregiver) shows only Notifications + Account sections — Family/Children sections are absent from the view tree, not just hidden"
  artifacts:
    - path: "androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/HeatmapView.kt"
      provides: "GitHub-style rolling heatmap calendar"
      contains: "fun HeatmapView"
      min_lines: 30
---

<objective>
Implement Stage 2 of Phase 5 (Core Event Logging) for the Android app: the History tab (heatmap + day-detail drill-down + per-event delete), the full Settings tab (child management, notification preferences, account actions), and the two backend pieces this depends on (`notification_preferences` GET/PUT endpoint, `PATCH /v1/children/{id}`). This is deferred from `05-01-PLAN.md` (Stage 1: theme, data layer, onboarding wizard, 4-tab shell, Home tab) due to phase size — not yet executed as of this plan's creation (2026-06-30).

Depends on Stage 1 (`05-01-PLAN.md`) being merged: theme system, SQLDelight driver/repositories, `AppContainer`, `ChildSelectionViewModel`, `MainTabNavigation`, and the `DestructiveConfirmDialog` component all need to exist first.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-core-event-logging/05-CONTEXT.md
@.planning/phases/04-ui-ux-design/04-UI-SPEC.md
@docs/WIREFRAMES.md
@docs/SCREEN-FLOWS.md
@docs/DESIGN-TOKENS.md
@docs/04-data-model.md
@.planning/phases/05-core-event-logging/05-01-PLAN.md
</context>

<decisions>
## Decisions carried from 05-CONTEXT.md relevant to this plan

- D-06: every History Day-Detail card is tappable, not just pending ones; "Delete event" lives inside the reopened Event Detail sheet using the standard `DestructiveConfirmDialog` shape (title "Remove event?" — no exact wireframe copy exists for this gap, follow the established title/body/confirm(error)/dismiss pattern).
- D-07: heatmap grows incrementally from week 1, caps at a fixed 12 weeks — no pre-rendered empty weeks, no pagination beyond 12 weeks.
- D-03: notification_preferences is a real table + upsert endpoint in Phase 5, not stubbed — Phase 8 only adds FCM-sending on top.
- D-08: caregiver removal is a direct Clerk SDK org-membership-removal call, no new Go endpoint.
- D-09: "Add child" from Settings repeats the full consent screen + inserts a fresh `consent_events` row, identical to onboarding step 3-4.
- Settings role gate (org:admin vs org:caregiver) removes Family/Children sections from the view tree entirely for caregivers, not just visually hides them (mirrors API-level enforcement).
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Backend — notification_preferences endpoint + PATCH /v1/children/{id}</name>
  <files>api/openapi.yaml, backend/internal/api/notification_preferences_handler.go, backend/internal/api/children_handler.go</files>
  <action>
    Add `GET /v1/notification-preferences` and `PUT /v1/notification-preferences` (or equivalent per-child upsert shape) and `PATCH /v1/children/{id}` to `api/openapi.yaml`. Run `make generate`. Implement handlers following `backend/internal/api/children_handler.go` conventions. Add tests mirroring `children_handler_test.go` / `erasure_handler_test.go`.
  </action>
</task>

<task type="auto">
  <name>Task 2: NotificationPreferences data layer + ApiClient</name>
  <files>shared/src/commonMain/sqldelight/com/onesteptwo/db/NotificationPreferences.sq, shared/src/commonMain/kotlin/com/onesteptwo/data/NotificationPreferencesRepository.kt, shared/src/commonMain/kotlin/com/onesteptwo/api/ApiClient.kt</files>
  <action>
    Create the `NotificationPreferences.sq` table + queries (mirrors backend schema). Build a lightweight `ApiClient` in `shared/commonMain/api/` wrapping the existing `HttpClientFactory`-built Ktor client with typed calls (`getChildren`, `patchChild`, `deleteChild`, `deleteAccount`, `getNotificationPreferences`, `putNotificationPreferences`) using hand-written `@Serializable` DTOs — do not introduce an oapi-codegen-generated Kotlin client this phase.
  </action>
</task>

<task type="auto">
  <name>Task 3: History tab — heatmap + day-detail + delete</name>
  <files>androidApp/src/main/kotlin/com/onesteptwo/android/ui/history/*.kt, androidApp/src/main/kotlin/com/onesteptwo/android/ui/home/EventDetailSheet.kt, androidApp/src/main/kotlin/com/onesteptwo/android/navigation/MainTabNavigation.kt</files>
  <read_first>
    - docs/WIREFRAMES.md Group D (History Tab — heatmap, empty state, Day-Detail)
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md Component 4 (Heatmap Calendar Cell) and the Accessibility Contract heatmap cell formats
  </read_first>
  <action>
    Build `HistoryViewModel` (daily counts for the active child via `PottyEventsRepository.selectDailyCounts`, rolling 12-week window starting at week 1). Build `HeatmapView` (week-day headers, month-row labels, intensity-colored cells per docs/DESIGN-TOKENS.md, a11y contentDescription formats from the Accessibility Contract). Build `HistoryScreen` (heatmap + empty state) and `DayDetailScreen` (pushed at `history/day/{date}`, tab bar hidden, chronological event cards, every card tappable). Extend `EventDetailSheet` with a "Delete event" destructive action using `DestructiveConfirmDialog`.
  </action>
</task>

<task type="auto">
  <name>Task 4: Settings tab — child management, notifications, account</name>
  <files>androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/*.kt</files>
  <read_first>
    - docs/WIREFRAMES.md Group E (Settings Tab — Admin View / Caregiver View)
    - .planning/phases/04-ui-ux-design/04-UI-SPEC.md Destructive Action Confirmations table (exact copy for Remove child / Remove caregiver / Delete my data)
    - androidApp/src/main/kotlin/com/onesteptwo/android/ui/settings/InviteCaregiverScreen.kt (existing, reuse as-is)
  </read_first>
  <action>
    Build `SettingsViewModel` (role-derived from `Clerk.organizationMembership?.role`, drives which sections render). Build `SettingsScreen`: admin sees Family (members + reused `InviteCaregiverScreen` route, remove-caregiver via direct Clerk SDK call per D-08), Children (`ChildManagementScreen`: list/add via repeated consent flow per D-09/edit via `PATCH`/remove via real `DELETE /v1/children/{id}` + `DestructiveConfirmDialog` using the exact table copy), Notifications (`NotificationPreferenceRow` toggle, persisted via Task 2's repository+endpoint), Account (email, sign out with no confirmation, "Delete my data" via real `DELETE /v1/account` + `DestructiveConfirmDialog`, reachable in 2 taps). Caregiver sees only Notifications + Account — Family/Children composables must not be present in the tree, not merely hidden.
  </action>
</task>

</tasks>

<verification>
- `./gradlew :androidApp:assembleDebug` succeeds after every task; `go test ./...` passes after Task 1.
- Manual: tap a non-empty heatmap cell -> Day-Detail list -> tap any card -> sheet pre-filled -> Delete -> confirm -> row gone, heatmap updates.
- Manual: toggle a notification preference -> kill and relaunch app -> toggle state persisted (round-trips through the new Go endpoint).
- Manual: remove a child -> confirm dialog exact copy matches the Destructive Action Confirmations table -> child and its events gone.
- Manual: sign in as a caregiver -> Settings shows exactly 2 sections (Notifications, Account) -> "Delete my data" reachable within 2 taps from the Settings tab.
</verification>
